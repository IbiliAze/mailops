export type Account = {
  id: string;
  label: string;
  email: string;
  employeeName: string;
  host: string;
  port: number;
  secure: boolean;
  user: string;
  pass: string;
  inbox: string;
  sent: string;
  createdAt: string;
  updatedAt: string;
};
