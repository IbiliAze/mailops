////////////////////////////////////////////////////////////////////////////////??PACKAGES
import { IsNotEmpty, IsString, Length } from 'class-validator'
////////////////////////////////////////////////////////////////////////////////////////??

export class CreateSummaryRequest {
  @IsString()
  @Length(10, 10)
  dateKey!: string

  @IsString()
  @IsNotEmpty()
  content!: string
}
